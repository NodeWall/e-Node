# Listen on the host IP so the CT (on vmbr0) can reach the broker (R1 fix),
# and on loopback so the host-side metrics-publisher can publish locally.
listener 1883 ENODE_HOST_IP
listener 1883 127.0.0.1
allow_anonymous true
persistence false
log_dest stdout
connection_messages false
